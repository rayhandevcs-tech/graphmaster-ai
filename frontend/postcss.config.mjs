/** Tailwind 4 is a PostCSS plugin; there is no `tailwind.config` — the theme
 *  lives in `app/globals.css` under `@theme`. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;

# Environment & secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
service-account*.json

# Python
__pycache__/
*.py[cod]
*.so
.Python
venv/
.venv/
env/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
.eslintcache
# Regenerated by `next build`; Next's own template ignores it.
next-env.d.ts

# Uploads & generated files
backend/storage/
backend/uploads/
backend/reports/
*.sqlite3

# ML model weights (baked into the Docker image, not committed).
# Anchored so these do not also match backend/app/models/, the ORM layer.
/ml-models/
/backend/ml-models/
.EasyOCR/

# OS & editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Logs
*.log
npm-debug.log*
logs/

# Test artefacts
.test-storage/

# Local-machine compose overrides (not shared)
docker-compose.override.yml
